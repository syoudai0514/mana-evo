# Mana Evo 正本設計スナップショット — 2026-08-24

この `design/` は PR #5 以降の実装レビューで仕様基準を固定するための**レビュー用正本スナップショット**です。

## 優先順位

1. このスナップショットに明記された、ユーザー承認済みの決定
2. `sol_game_review_for_claude_v3.md` の最新訂正方針
3. `sol_game_review_for_claude_v2.md`
4. 上記と矛盾しない最新モック
5. PR内の実装状況文書

PRの `UNRESOLVED` は設計を上書きしません。過去に確定した仕様が後続のレビュー資料で一時的に未決扱いされていても、根拠資料から復元できたものは確定仕様へ戻します。

## PR #15 での正本ファイル優先順位

実装・レビューでは以下を優先して参照します。

- `design/11-battle-character-boss-review.md`
  - 238体正式マスター投入前の再レビュー確定差分
  - 通常敵/ボスのreferencePower分離、XP実装、8ロール、技帯域、ボスAI、捕獲XP、UI修正
  - `06` / `10` と差がある場合は `11` を優先
- `design/09-special-forms-master.md`
  - ギガシンカ対象12体、キョダイバースト対象8体の具体No./名前/タイプ
  - ギガキー/ギガコア/バーストのしるし、効果、排他条件
- `design/10-initial-balance-master.md`
  - XP、レベル能力値、技威力、捕獲率、通常敵/ボス倍率の**初期実装値**
  - 調整担当、調整トリガー、main merge前の必須バランスゲート
  - `08-balance-tuning-policy.md` と差がある場合は `10` を優先
- `design/06-battle-and-progression-design.md`
  - バトル、育成、敵/ボスの基本構造
- `design/08-balance-tuning-policy.md`
  - バランス調整運用の補足。数値競合時は `10` を優先
- `01-UNRESOLVED-DECISIONS.md`
  - 本当に残っている未決事項だけを記録。確定済み仕様を未決へ戻さない

## 重要

- `スター覚醒` は不採用。現行仕様へ再導入しない。
- 特殊形態は `ギガシンカ` / `キョダイバースト`。
- ギガ12体・バースト8体は実装前設計から復元済みで、再選定しない。
- Kids Quest本体は変更しない。学習部分のみMana Evoへ再利用する。
- 正式キャラ画像は別工程。正式画像が完成済みのキャラは正式画像、未完成キャラはplaceholderで進め、ゲームロジックを画像待ちにしない。
- バトルの能力式・敵/ボス調整は `11` → `06` + `10` の順で参照する。
- 本番中に子ども個人へ合わせてXP/技威力/捕獲率/敵倍率を自動変更しない。

## 現在の実装状態に関する注意

PR #15には戦闘バランス基盤と設計正本は入っていますが、**No.001〜238の正式モンスターマスター本体、正式ID migration、QAモード、238体画像registryはまだ未投入**です。

設計文書に238体ルールが書かれていることと、238体実装が完了していることを混同しないでください。実装完了判定はPRの変更ファイルとCI/QA結果で行います。

## このスナップショットの限界

元の `02-dex-200.md` 全本文を再生成したものではありません。キャラクターの最終名・全系列について、過去資料で確認できていない部分を勝手に補完しません。

実装前設計で確定していた情報を再回収した場合は、根拠を確認して正本へ戻します。レビュー補助資料の `UNRESOLVED` を理由に、過去に確定していた仕様を未決へ戻しません。

各ファイルのSHA-256は `DESIGN-SOURCE-METADATA.txt` に記録します。
