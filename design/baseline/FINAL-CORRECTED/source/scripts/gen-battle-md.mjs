import fs from 'node:fs'
import { FAMILIES } from './families.mjs'
import { TYPES, TYPE_BY_ID } from './types.mjs'
import { GIGA_SHINKA, KYODAI_BURST, GIGA_STAT_MULT, BURST_STAT_MULT_HP, BURST_STAT_MULT_ATK, BURST_MOVE, NORMAL_FINAL_MOVE } from './forms.mjs'
import { totalXp, xpBetween, DAY, WILD, BOSS, AREA_UNLOCK_DAY, baseStats, familyBaseStats, realHp, realStat, damage, ROLE_SPLIT,
  BOSS_TELEGRAPH_POWER, BOSS_ULTIMATE, GUARD_MOVE, AI_READS_PLAYER_INPUT } from './battle.mjs'
import { UNLOCK_AREA, STONE_LABEL, HOLD_LABEL, ACQUIRE_RULE } from './items.mjs'
import { RATE, RANK_LABEL, BALL, singleThrowRate, cumulativeRate, displayLabel } from './capture.mjs'
import { encounterInfo, WILD_WEIGHT, ENCOUNTER_TUNING, ENCOUNTER_LOCK } from './wildEncounter.mjs'
import { REWARDS, DIFFICULTY } from './rewards.mjs'
import { simulateDexCompletion } from './simulate.mjs'

const ROLES = { attacker:'アタッカー', guard:'ガード', healer:'ヒーラー', support:'サポート', speed:'スピード', balanced:'バランス' }
const L = []; const w = (s='') => L.push(s)

w('# バトルと成長の設計')
w()
w('> **数値はすべてこのディレクトリのスクリプトで計算し、`node scripts/battle.mjs` 等で再現できます。** 文章だけの目安ではありません。')
w('> **状態遷移・地域解放・探索・重複捕獲は `08-gameplay-state-spec.md` を優先する。**')
w()
w('## 0. この文書で決めたこと（要約）')
w()
w('| 項目 | 決定 |')
w('|---|---|')
w('| ステータス | **4つだけ**（たいりょく / こうげき / ぼうぎょ / すばやさ）。とくこう・とくぼうは作らない |')
w('| 種族値（BST） | **「第1形態か・中間形態か・最終形態か」で決める**（旧仕様は段数で決めていたため、2段階系列の最終形が中間形態相当の値になっていたバグを修正） |')
w('| 進化とステータス | **進化で1つもステータスが下がらない**ことを155件全数で機械検証 |')
w('| ダメージ式 | マナエボの簡約式。乱数は 0.90〜1.00。**タイプ相性0のときは必ず0ダメージ** |')
w('| タイプ一致（STAB） | **入れる**。×1.5 |')
w('| タイプ相性 | `scripts/types.mjs` の18タイプ相性表を正本として使う |')
w('| 急所 | 1/16 で ×1.5。プレイヤー側・敵側とも同じ確率 |')
w('| 状態異常 | **4つだけ**（やけど・まひ・どく・ねむり）。こんらんは入れない。**捕獲率には一切影響しない**（下記） |')
w('| わざ | 1体4つ。レベルで覚える。5つ目を覚えるときだけ入れ替え確認 |')
w('| まもる | **正式採用。** そのターンのダメージを100%カット。**使用後1ターン再使用不可**（§6） |')
w('| 手持ち | 3体。場に1体。交代はいつでも1回ぶんの行動 |')
w('| 1戦の長さ | 通常 45〜90秒（4〜6発で決着）／ボス 2〜3分 |')
w('| 敵レベル | **エリアごとに固定**。プレイヤーに追従させない |')
w('| 敵AI | **プレイヤーの入力を見てから行動を変えない**（＝入力読み禁止。旧仕様の「交代先に合わせる」を削除） |')
w('| ボスの大技 | 予告1ターン＋大技（威力170・命中90%）。2ターン平均が通常わざ連打より明確に強い |')
w('| 捕獲 | **勝利→捕獲フェーズ→わ→最大3投の方式Aに一本化**。HP残量・状態異常による捕獲補正は撤廃（詳細は `07-wild-encounter-and-capture-design.md`） |')
w('| ギガシンカ | **全ステータス×1.35 の1つに統一**。種族ごとにギガ専用（キョダイと排他） |')
w('| キョダイバースト | たいりょく×2.0／こうげき×1.2、バーストわざ（威力110・命中95%・反動なし）。種族ごとにバースト専用 |')
w('| 学習報酬 | ほしのわ・ぎんのわ・チケット・きんのわの発生条件を `rewards.mjs` に一本化（01と06の食い違いを解消） |')
w('| 追加チケット | **上限なし**。難易度の自動上昇（習熟度ベース）で歯止めをかける |')
w('| 難易度上昇 | 「回数」ではなく**直近8問の正答率**で調整。連打・同一選択肢の当てずっぽうは加算しない |')
w('| XP曲線 | `total(L) = round(6 × (L−1)^1.9)` |')
w('| 標準1日XP | 350（手持ち3体すべてに同額） |')
w('| 進化までの日数 | レベル進化123件・アイテム進化32件、あわせて155件すべてを検証（下の§10） |')
w()
w('---')
w()

// ===== 1. ステータス =====
w('## 1. ステータス')
w()
w('### なぜ4つだけにしたか')
w()
w('マナエボでは、5〜8歳が覚える数を増やしすぎないため、とくこう／とくぼうを分けない。')
w('理由は「5歳が画面を見て、なぜ効いたのかを自分で説明できる」ことを守りたいから。')
w('わざが物理か特殊かを覚えるより、**タイプ相性を考えることに脳を使ってほしい**。')
w('タイプ相性を考える部分を、バトルの中心的な面白さとして残す。')
w()
w('| ステータス | 画面の呼び方 | はたらき |')
w('|---|---|---|')
w('| HP | **たいりょく** | 0になると たおれる |')
w('| ATK | **こうげき** | あたえるダメージ |')
w('| DEF | **ぼうぎょ** | うけるダメージを へらす |')
w('| SPD | **すばやさ** | 高いほうが先に動く。同値なら乱数 |')
w()
w('### 種族値の決め方')
w()
w('種族値の合計（BST）は、**そのモンスターが系列内で「第1形態」「中間形態（3段階のみ）」「最終形態」の')
w('どれに当たるか**で決まる。')
w()
w('> **何が問題だったか。** 旧実装は BST を「そのステージ番号」でそのまま引いていた')
w('> （1段目=200 / 2段目=270 / 3段目=340）。すると **2段階系列の最終形が「2段目」＝270** になり、')
w('> 3段階系列の最終形340と大差が付いた。**2段階系列は「最終形なのに中間形相当」の弱さになっていた。**')
w('> ステージ番号ではなく「最終形かどうか」で決め直したことで、2段階でも3段階でも最終形は同じ340になる。')
w()
w('| 位置づけ | 基準BST |')
w('|---|---|')
w('| 第1形態（2段階・3段階とも） | 200 |')
w('| 中間形態（3段階のみ） | 270 |')
w('| **最終形態（2段階・3段階とも）** | **340** |')
w('| 単体（進化なし） | 380 |')
w()
w('| ランク | 倍率 |')
w('|---|---|')
w('| ふつう | ×0.95 |')
w('| めずらしい | ×1.00 |')
w('| とてもめずらしい | ×1.08 |')
w('| でんせつ | ×1.20 |')
w()
w('合計をロールで配る。**ロールは飾りではなく、この配分そのもの**。')
w()
w('| ロール | HP | こうげき | ぼうぎょ | すばやさ | 役目 |')
w('|---|---|---|---|---|---|')
const ROLE_DESC = { attacker:'とにかく殴る。打たれ弱い', guard:'受けて耐える。倒しきれない', speed:'先手を取る。紙', healer:'味方を回復する。攻撃は苦手', support:'状態異常と補助。器用', balanced:'穴がない。最初の相棒向き' }
for (const [k, s] of Object.entries(ROLE_SPLIT)) {
  w(`| ${ROLES[k]} | ${(s.hp*100).toFixed(0)}% | ${(s.atk*100).toFixed(0)}% | ${(s.def*100).toFixed(0)}% | ${(s.spd*100).toFixed(0)}% | ${ROLE_DESC[k]} |`)
}
w()
w('### 進化で1つもステータスが下がらない')
w()
w('> **何が問題だったか。** 155件の進化ステップのうち52件はロール（役割）も変わる')
w('> （例: アタッカー→ガード）。BSTが増えていても、配分比率の変化幅の方が大きいと、')
w('> 素の計算では一部ステータスが下がることがあった')
w('> （例: ワカバネ→ジュランガ で こうげき 70→68、すばやさ 65→48）。')
w()
w('修正方針: 各ステータスを**「1つ前の形態の値未満にはしない」よう底上げ**する。')
w('BST・ロール配分の計算はそのままに、結果だけを底上げするので、')
w('「進化してもロールの個性は保ちつつ、どのステータスも必ず伸びる」の両立になる。')
w()
w('| モコハ系列（くさ・スターター） | HP | こうげき | ぼうぎょ | すばやさ |')
w('|---|---|---|---|---|')
{
  const moko = FAMILIES.find((f) => f.members[0].name === 'モコハ')
  const stats = familyBaseStats(moko)
  for (const s of stats) w(`| ${s.name} | ${s.hp} | ${s.atk} | ${s.def} | ${s.spd} |`)
}
w()
w('（底上げが効いた箇所: ジュランガの こうげき・すばやさ は、ロール変更前の素の計算値より高く底上げされている）')
w()
w('この底上げロジックを含めて、**155件の進化ステップすべてでステータス非減少**を `check2.mjs` で検証している。')
w()
w('### レベルごとの実数値')
w()
w('```')
w('たいりょく = floor(種族値 × Lv ÷ 50) + Lv + 10')
w('その他     = floor(種族値 × Lv ÷ 50) + 5')
w('```')
w()
w('個体値・努力値は入れない。同じ種族なら同じレベルで必ず同じ数値になる。')
w('「なぜか弱い個体」を引かないので、子どもが自分の育て方を疑わずに済む。')
w()
w('| 例 | 種族値（HP/こう/ぼう/すば） | Lv.10 | Lv.30 | Lv.50 |')
w('|---|---|---|---|---|')
const EX = [
  ['ふつう・第1形態・バランス', 1, 3, 'common', 'balanced'],
  ['めずらしい・最終形態・ガード', 3, 3, 'rare', 'guard'],
  ['とてもめずらしい・最終形態・アタッカー', 3, 3, 'epic', 'attacker'],
  ['でんせつ・単体・バランス', 1, 1, 'legend', 'balanced'],
]
for (const [lab, st, mx, rk, ro] of EX) {
  const b = baseStats(st, mx, rk, ro)
  const at = (Lv) => `HP${realHp(b.hp,Lv)} / ${realStat(b.atk,Lv)} / ${realStat(b.def,Lv)} / ${realStat(b.spd,Lv)}`
  w(`| ${lab} | ${b.hp}/${b.atk}/${b.def}/${b.spd}（計${b.bst}） | ${at(10)} | ${at(30)} | ${at(50)} |`)
}
w()
w('---')
w()

// ===== 2. ダメージ式 =====
w('## 2. ダメージ式')
w()
w('```')
w('base   = floor( floor( (2 × Lv ÷ 5 + 2) × いりょく × こうげき ÷ ぼうぎょ ) ÷ 50 ) + 2')
w('ダメージ = floor( base × タイプ一致 × 相性 × 急所 × 乱数 )')
w()
w('タイプ一致 = 1.5（じぶんと同じタイプのわざ）／ それ以外 1.0')
w('相性       = 2 / 1 / 0.5 / 0（18タイプ表）')
w('急所       = 1.5（1/16の確率）')
w('乱数       = 0.90 〜 1.00')
w('最低ダメージ = 1。ただし相性が0（こうかなし）のときは必ず0')
w('```')
w()
w('> **仕様上の注意:** 旧実装は `Math.max(1, ...)` を無条件にかけていたため、')
w('> 「こうかなし」のはずのわざでも1ダメージ入ってしまうバグがあった。')
w('> `type === 0` のときは他の計算より先に0を返すよう直し、`check2.mjs` に専用テストを追加した。')
w()
w('### 同じくらいの相手と殴り合ったとき（いりょく60・等倍・一致なし）')
w()
w('| レベル | 1発のダメージ | 相手のHP | 何発で たおれる |')
w('|---|---|---|---|')
for (const Lv of [10, 20, 30, 40, 50]) {
  const st = Lv < 18 ? 1 : Lv < 34 ? 2 : 3
  const a = baseStats(st, 3, 'rare', 'attacker'), d = baseStats(st, 3, 'rare', 'guard')
  const dmg = damage({ level: Lv, power: 60, atk: realStat(a.atk, Lv), def: realStat(d.def, Lv) })
  const hp = realHp(d.hp, Lv)
  w(`| Lv.${Lv} | ${dmg} | ${hp} | ${(hp/dmg).toFixed(1)}発 |`)
}
w()
w('**4〜6発で決着**する。1ターン約8秒として、交代や演出を入れて 45〜90秒。')
w()
w('### 相性を分かって戦うと、どれくらい変わるか')
w()
w('Lv.30・いりょく60・アタッカー → ガード（相手HP 105）で計算。')
w()
w('| 状況 | ダメージ | 相手HPの | 何発 |')
w('|---|---|---|---|')
{
  const a = realStat(baseStats(3,3,'rare','attacker').atk, 30)
  const d = realStat(baseStats(3,3,'rare','guard').def, 30)
  const hp = realHp(baseStats(3,3,'rare','guard').hp, 30)
  const cases = [
    ['なにも考えず殴る', 1, 1, 1], ['タイプ一致のわざ', 1.5, 1, 1],
    ['ばつぐんのわざ', 1, 2, 1], ['**一致＋ばつぐん**', 1.5, 2, 1],
    ['一致＋ばつぐん＋急所', 1.5, 2, 1.5], ['いまひとつ', 1, 0.5, 1],
    ['**こうかなし**', 1, 0, 1],
  ]
  for (const [lab, stab, ty, cr] of cases) {
    const dmg = damage({ level: 30, power: 60, atk: a, def: d, stab, type: ty, crit: cr })
    w(`| ${lab} | ${dmg} | ${(dmg/hp*100).toFixed(0)}% | ${dmg === 0 ? '当たらない' : Math.ceil(hp/dmg)+'発'} |`)
  }
}
w()
w('**考えて選ぶと6発が2発になる。こうかなしを選ぶと0発（＝当たらない）。**')
w('わざを選ぶ前に ◎ ○ △ × を出すので、読めなくても分かる。')
w()
w('---')
w()

// ===== 3. 相性表示 =====
w('## 3. タイプ相性の見せ方')
w()
w('| 表示 | 倍率 | 音 |')
w('|---|---|---|')
w('| ◎ ばつぐん | ×2 | 高く短い「キン！」 |')
w('| ○ ふつう | ×1 | 通常 |')
w('| △ いまひとつ | ×0.5 | 低く鈍い「ボスッ」 |')
w('| × きかない | ×0 | 無音＋画面が一瞬グレー |')
w()
w('- わざボタンに常時つける。押す前に分かる')
w('- タイプは**アイコンと色を主**にする（' + TYPES.slice(0,6).map(t=>`${t.emoji}${t.name}`).join(' / ') + ' …）')
w('- 相手のタイプは画面上部に大きく1つ出す')
w('- **相性を隠さない。** すでに知っている子から、知っている楽しみを取り上げない')
w()
w('### 複合タイプについて')
w()
w('**v1 は全員が単タイプ。複合タイプは v2 で入れる。** 理由:')
w()
w('1. ダメージ式には最初から `相性 = eff(わざ, タイプ1) × eff(わざ, タイプ2)` と書いておく。データに `type2` を足すだけで動く')
w('2. 先に決めるべきは複合タイプではなく「4〜6発で決着するか」「進化が何日か」で、そこは単タイプでも検証できる')
w('3. 今回の実装では単タイプに固定し、複合タイプは将来拡張として扱う')
w()
w('v2 で複合にする候補は先に決めておく（エリア3〜4の16系列）。')
w()
w('| 系列 | 現在 | 追加するタイプ | 理由 |')
w('|---|---|---|---|')
const DUAL = [
  ['ヒョウガルド','こおり','あく','氷の獣。獰猛さを足す'],
  ['ダイヤグレス','こおり','いわ','結晶＝鉱物'],
  ['ヒョウテイン','こおり','みず','ペンギンは泳ぐ'],
  ['カイテイリオ','みず','ドラゴン','海竜'],
  ['センジュガ','くさ','エスパー','千手の御神木'],
  ['ハサミガルダ','むし','はがね','クワガタの外骨格'],
  ['カブトレクス','むし','かくとう','カブトムシの力比べ'],
  ['ミチシルベ','ゴースト','ほのお','提灯の灯'],
  ['ミツアシガ','あく','ひこう','三本足のカラス'],
  ['キュウビガミ','あく','フェアリー','九尾の妖狐'],
  ['ガードヴァルツ','はがね','かくとう','鋼の守り手'],
  ['イワガミラ','いわ','じめん','崖そのもの'],
  ['テラガイア','じめん','ドラゴン','大地の竜'],
  ['ジャドクラ','どく','ドラゴン','大蛇'],
  ['テンショウガ','ひこう','かくとう','猛禽の爪'],
  ['ブルーメルナ','フェアリー','くさ','花の精'],
]
for (const [n, t1, t2, why] of DUAL) w(`| ${n} | ${t1} | ${t2} | ${why} |`)
w()
w('いずれも**最終形だけ**に足す。進化して急に弱点が増える／減るのは、')
w('「進化で何かが変わった」を分かりやすくするうえでもプラスになる。')
w()
w('---')
w()

// ===== 4. わざ =====
w('## 4. わざ')
w()
w('### 構成')
w()
w('1体が同時に持てるのは4つ。5つ目を覚えるときだけ「どれと入れ替える？」を聞く。')
w('**忘れさせたわざは「おもいだす」でいつでも戻せる。**')
w('5歳が押し間違えて主力を消す事故を、確認ダイアログではなく巻き戻しで防ぐ。')
w()
w('| いりょく | 位置づけ | 例 |')
w('|---|---|---|')
w('| 40 | 追加効果つきの小技。命中100 | 状態異常を狙う |')
w('| 60 | 標準。序盤〜中盤の主力。命中100 | タイプ一致で使う |')
w(`| ${NORMAL_FINAL_MOVE.power} | 強い。命中${Math.round(NORMAL_FINAL_MOVE.accuracy*100)} | 最終形の主力 |`)
w('| 100 | もっと強い。命中90 | 決めにいく |')
w(`| ${BURST_MOVE.power} | バーストわざ専用。命中${Math.round(BURST_MOVE.accuracy*100)}・**反動なし** | キョダイバースト中のみ（§11） |`)
w()
w('> **仕様上の注意:** 旧仕様は「バーストわざ＝威力120・命中90・次のターン動けない」で、')
w('> 2ターン平均の期待値が通常わざ連打より弱くなり得た。**「次のターン動けない」を撤廃**し、')
w('> 威力と命中を調整（EV=104.5）して通常の主力わざ（EV=76）を明確に上回るよう固定した（§11で検算）。')
w()
w('補助わざ（ダメージ0）は「こうげきアップ」「ぼうぎょアップ」「かいふく」「状態異常」「**まもる**」の5系統。')
w('まもるの正式仕様は §6 を参照。')
w()
w('### 覚えるレベル')
w()
w('進化レベルに合わせて決める。**進化した直後に必ず1つ覚える**ようにして、')
w('「進化したら新しいわざが増えた」を毎回体験させる。')
w()
w('| タイミング | 覚えるもの |')
w('|---|---|')
w('| つかまえた時 | ノーマルの小技（40）＋ 自分のタイプの小技（40）＋ まもる |')
w('| 1だんかい目の進化＋0 | 自分のタイプの標準わざ（60） |')
w('| 1だんかい目の進化＋5 | 補助わざ（ロールに対応） |')
w('| 2だんかい目の進化＋0 | 自分のタイプの強わざ（80） |')
w('| 2だんかい目の進化＋6 | サブタイプの標準わざ（60） |')
w('| 最終進化＋8 | **専用わざ（100）** — その子だけのわざ |')
w()
w('専用わざは名前も専用にする。図鑑の「わざ」欄に、まだ覚えていなくても')
w('**名前だけ先に出す**（効果は「？？？」）。これが育てる目標になる。')
w()
w('### ロールと補助わざの対応')
w()
w('| ロール | 覚える補助わざ |')
w('|---|---|')
w('| アタッカー | こうげきアップ（自分・2段階） |')
w('| ガード | ぼうぎょアップ（自分・2段階） |')
w('| スピード | すばやさアップ＋つぎの一撃が必ず急所 |')
w('| ヒーラー | かいふく（自分の最大HPの1/2） |')
w('| サポート | 相手を まひ／どく にする |')
w('| バランス | 相手のこうげきダウン |')
w()
w('---')
w()

// ===== 5. 状態異常 =====
w('## 5. 状態異常')
w()
w('4つだけ。**こんらん（自分を攻撃してしまう）は入れない。** 理不尽に見えるため。')
w('**捕獲率には一切影響しない**（旧仕様の「ねむり・まひで捕獲率アップ」は方式Aへの一本化に伴い撤廃。§8）。')
w()
w('| 状態 | 効果 | 続く長さ | 見せ方 |')
w('|---|---|---|---|')
w('| やけど 🔥 | こうげき ×0.7 ／ 毎ターン 最大HPの1/16 | バトル終了まで | 赤い炎のアイコン |')
w('| まひ ⚡ | すばやさ ×0.5 ／ 25%で うごけない | バトル終了まで | 黄色い火花 |')
w('| どく 🟣 | 毎ターン 最大HPの1/8 | バトル終了まで | 紫のあわ |')
w('| ねむり 💤 | うごけない | 1〜3ターン（攻撃を受けると起きる） | 青いZ |')
w()
w('- **バトルが終わればすべて治る。** 街に戻って薬を買う手順を作らない')
w('- 同じ状態は重ならない。すでにやけどの相手にやけどは入らない（「きかなかった」と出す）')
w('- ほのおタイプは やけどにならない／でんきタイプは まひにならない／どく・はがねは どくにならない')
w('- サポートロールの価値は**純粋にバトル内の駆け引き**（相手の行動を縛る）に一本化した。捕獲との連動は無い')
w()
w('---')
w()

// ===== 6. 交代・行動順・まもる =====
w('## 6. 手持ち・交代・行動順・まもる')
w()
w('| 項目 | 仕様 |')
w('|---|---|')
w('| 手持ち | 3体。ボックスからいつでも入れ替え（バトル外） |')
w('| 場に出る | 1体 |')
w('| 交代 | 1ターンぶんの行動を使う。相手より先に処理される |')
w('| たおれたとき | 交代は無料（ターンを消費しない） |')
w('| 全滅 | 「まけちゃった」→ その場でHP全快。ペナルティなし。チケットも減らない |')
w('| 行動順 | すばやさが高いほう。同値は乱数 |')
w('| にげる（バトル中） | 野生からはいつでも成功。ボスからは にげられない |')
w()
w('**負けても失うものを作らない。** 失うのは「今回はゲットできなかった」だけ。')
w('チケットが減らないので、「もう一回！」がすぐ押せる。')
w()
w('> ※ 敗北画面の表示報酬と実際の付与結果が食い違わないよう、表示も同じ報酬計算結果を参照する。')
w('> 新ゲームでは **負けの報酬を0にして表示もしない** ことで構造的に消す。')
w()
w('### まもる')
w()
w('前バージョンはボス戦UIに「こうたい／まもる／押し切る」が突然出てくるのに、')
w('通常バトルアクションとして「まもる」が定義されていなかった。今回、以下で完全に確定する。')
w()
w('| 項目 | 仕様 |')
w('|---|---|')
w(`| 使用条件 | **いつでも選べる**。PP・ゲージの消費なし |`)
w(`| 効果 | そのターンに受けるダメージを **100%カット**（状態異常わざも防ぐ） |`)
w('| 連続使用 | **不可**。使用後1ターンは選べない。「つぎは まもるを つかえないよ！」と表示 |')
w(`| 成功率 | **100%**。外れない |`)
w('| ボス大技との関係 | ボスの「予告」ターンに使うと、大技のダメージを確実に0にできる |')
w()
w('毎ターン連打しても「安全なだけで勝てない」ので、追加の失敗率が無くても')
w('自然と「大技を防ぐ」用途に使いどころが絞られる。中途半端な仕様のまま残さない。')
w()
w('---')
w()

// ===== 7. 敵 =====
w('## 7. 敵のレベルとAI')
w()
w('### レベル帯（プレイヤーに追従させない）')
w()
w('**敵レベルはエリアで固定**する。')
w()
w('| エリア | 野生のレベル | ボス | 出る だんかい |')
w('|---|---|---|---|')
for (const a of [1,2,3,4]) {
  w(`| ${a} | Lv.${WILD[a][0]}〜${WILD[a][1]} | Lv.${BOSS[a]} | ${a<=2 ? '1だんかい目のみ' : '1〜2だんかい目'} |`)
}
w()
w('**最終形は野生に出ない。** 進化させないと手に入らない。')
w()
w('### 「育てたら本当に強い」の検算')
w()
w('Lv.40 まで育てた最終形（めずらしい・アタッカー）を、エリア1の野生 Lv.6 にぶつける。')
w()
{
  const strong = baseStats(3,3,'rare','attacker'), weak = baseStats(1,3,'common','balanced')
  const d = damage({ level:40, power:60, atk:realStat(strong.atk,40), def:realStat(weak.def,6), stab:1.5 })
  const back = damage({ level:6, power:60, atk:realStat(weak.atk,6), def:realStat(strong.def,40) })
  w('| | ダメージ | 相手のHP | 結果 |')
  w('|---|---|---|---|')
  w(`| こちらの一撃 | ${d} | ${realHp(weak.hp,6)} | **一撃で たおせる** |`)
  w(`| 相手の一撃 | ${back} | ${realHp(strong.hp,40)} | ${Math.floor(realHp(strong.hp,40)/back)}発 耐える |`)
}
w()
w('圧倒的になる。これが「強くなった」の正体で、レベル追従型では絶対に出せない感覚。')
w('捕獲は勝利後の別フェーズ（§8）なので、**一撃で倒してしまっても捕獲チャンスは減らない**。')
w('育てた子で序盤の未捕獲を埋めにいく行動が、そのまま気持ちよく成立する。')
w()
w('### 野生AI（かんたん）')
w()
w('1. 相性が ◎ のわざがあれば 70% でそれを選ぶ')
w('2. なければ いりょくが一番高いわざ')
w('3. HPが1/4以下になると 20% で「にげる」を試みる（レアほど逃げやすい）')
w()
w('相性を無視して殴ってこないので、**こちらのタイプ選択が意味を持つ**。')
w('ただし70%止まりなので、たまに変なわざを撃つ。ここで勝てる余地を残す。')
w()
w('### ボスAI（かんがえる・入力読み禁止）')
w()
w('> **仕様上の注意:** 旧仕様「プレイヤーが交代したら、交代先に有利な技を使う」は、')
w('> プレイヤーの入力を見てから行動を変える「入力読み」であり、5歳には「考えたのに敵がズルした」と')
w('> 感じられる。**敵AIは、プレイヤーがそのターンの行動を選ぶ前に、内部で自分の行動を先に決定する**')
w(`> （\`AI_READS_PLAYER_INPUT = ${AI_READS_PLAYER_INPUT}\` として設計上明示。battle.mjs参照）。`)
w()
w('1. 相性が ◎ のわざがあれば **必ず** それを選ぶ（ただし行動決定はプレイヤーの選択より前）')
w('2. HPが半分を切ると1回だけ「こうげきアップ」を使う')
w('3. 状態異常は半分の確率でしか入らない')
w()
w('### ボスの大技')
w()
w('> **何が問題だったか。** 旧仕様「1ターン溜め→威力100」は、溜めターンに何もしないため、')
w('> 2ターン平均の期待値が通常わざ連打より弱くなり得た。「大きいのが来る！」と予告するなら、')
w('> 通常わざより明確に強くなければならない。')
w()
w('修正後は、予告ターンもボスは黙って止まらず、控えめな威力で普通に攻撃する。')
w()
w('| ターン | ボスの行動 | 威力 |')
w('|---|---|---|')
w(`| 予告ターン | 画面が赤く光り「つぎのターン、おおきいのが くる！」＋ 通常の一撃 | ${BOSS_TELEGRAPH_POWER} |`)
w(`| 次のターン | **大技発動** | ${BOSS_ULTIMATE.power}（命中${Math.round(BOSS_ULTIMATE.accuracy*100)}%） |`)
w()
{
  const evUltimateSeq = (BOSS_TELEGRAPH_POWER + BOSS_ULTIMATE.power * BOSS_ULTIMATE.accuracy) / 2
  const evNormalSpam = NORMAL_FINAL_MOVE.power * NORMAL_FINAL_MOVE.accuracy
  w(`2ターン平均の期待値 = (${BOSS_TELEGRAPH_POWER} + ${BOSS_ULTIMATE.power}×${BOSS_ULTIMATE.accuracy}) / 2 = **${evUltimateSeq.toFixed(1)}**`)
  w(`ボスが通常わざ（威力${NORMAL_FINAL_MOVE.power}・命中${Math.round(NORMAL_FINAL_MOVE.accuracy*100)}%）を連打したときの期待値 = **${evNormalSpam.toFixed(1)}**`)
  w()
  w(`**${evUltimateSeq.toFixed(1)} > ${evNormalSpam.toFixed(1)}** なので、予告どおり通常わざ連打より明確に強い。`)
}
w()
w('予告ターン中に、プレイヤーは §6 の**まもる**（大技を確実に0にする）・**こうたい**（交代して被弾を避ける）・')
w('**わざで押し切る**（一気に決めにいく）のどれかを選べる。**ボス戦の山場はこの1択。**')
w()
w('ボスHPは同レベル帯の1.6倍。2〜3分で決着する。')
w()
w('---')
w()

// ===== 8. 捕獲との接続 =====
w('## 8. バトルと捕獲のつなぎ（方式Aに一本化）')
w()
w('> **仕様上の注意:** 旧文書には')
w('> 「HPを削って赤くする→てかげんでHP1に制限→ねむり/まひで捕獲率アップ→戦闘中に捕獲」（方式B）と')
w('> 「勝ってから、たおした相手に わ を投げる」（方式A）の**2つの捕獲フローが混在**していた。')
w('> 本作は **方式Aに一本化**する。詳しい捕獲式・演出・野生出現テーブルは')
w('> `07-wild-encounter-and-capture-design.md` に切り出した。ここではバトルとの接続だけを述べる。')
w()
w('```')
w('通常のバトルを行う')
w('  ↓')
w('敵HPを0にして勝利する（「たおした！」）')
w('  ↓')
w('捕獲フェーズへ移行（バトル画面から切り替わる）')
w('  ↓')
w('わ を選ぶ → 最大3回まで捕獲判定 → 成功ならゲット／3回失敗なら にげられた')
w('```')
w()
w('**捕獲判定は残りHP・状態異常に一切依存しない**（勝利した時点でHPは0のため、依存させる意味がない）。')
w('したがって「てかげん」「HPを1残す」「ねむり・まひで捕獲率アップ」の仕組みは撤廃した。')
w()
w('この一本化の副作用として、§7の「育てたら本当に強い」問題が自然に解決する。')
w('**一撃で倒してしまっても、捕獲フェーズは変わらず始まる**ので、育った子で序盤を周回して')
w('未捕獲を埋める行動が何の障害もなく成立する。')
w()
w('---')
w()

// ===== 9. XP =====
w('## 9. XP と成長速度')
w()
w('### 曲線')
w()
w('```')
w('つぎのレベルまでの合計XP: total(L) = round( 6 × (L − 1)^1.9 )')
w('```')
w()
w('| Lv | 合計XP | Lv | 合計XP | Lv | 合計XP |')
w('|---|---|---|---|---|---|')
{
  const ls = [5,10,15,20,25,30,35,40,45,50,55,60]
  for (let i = 0; i < ls.length; i += 3) {
    w('| ' + ls.slice(i,i+3).map(x=>`Lv.${x} | ${totalXp(x).toLocaleString()}`).join(' | ') + ' |')
  }
}
w()
w('指数が 2 ではなく **1.9** なのが要点。Kids Quest参照実装は `6×(L−1)²` で、')
w('2段目の進化に **19日** かかっていた（実測）。1.9に落とすと高レベルほど効いて、')
w('最終進化が目安の日数内に収まる。')
w()
w('### 学習報酬')
w()
w('> **何が問題だったか。** `01-catch-and-evolution-design.md` は「ほしのわ=コアタスク1つごと、')
w('> ぎんのわ=コア全クリア」、この文書は「チケット=コア5つで3枚、ぎんのわ=おかわり学習」と、')
w('> **同じ行動に対して違う報酬の付き方**を書いていた。以降は `rewards.mjs` を唯一の正本とし、')
w('> 01・06・新設の07とも、そこから生成する。')
w()
w('| きっかけ | もらえるもの | 上限 |')
w('|---|---|---|')
for (const [key, r] of Object.entries(REWARDS)) {
  const grants = r.perOccurrence || r.onceGrant || r.grant || {}
  const labels = {star:'ほしのわ',ticket:'バトルチケット',gold:'きんのわ',silver:'ぎんのわ',moonStone:'つきのいし',rainbow:'にじのわ',itemOfArea:'そのエリアのいし1種',explorePoint:'たんけんポイント'}
  const gain = Object.entries(grants).map(([k,v]) => `${labels[k]||k} +${v}`).join('、')
  const cap = r.dailyCap === null ? '**上限なし**' : r.dailyCap ? `1日${r.dailyCap}回まで` : '達成ごとに1回'
  w(`| ${r.label} | ${gain} | ${cap} |`)
}
w()
w('| もらえるXP | 値 |')
w('|---|---|')
w('| 1問 正解 | 4 |')
w('| コアタスク1つ 完了 | 20 |')
w('| バトル 勝利 | 40〜80（相手のレベルによる） |')
w('| ボス 勝利 | 300（初回のみ） |')
w()
w('**XPは手持ち3体すべてに同額入る。** 「出していない子が育たない」を作らない。')
w('5歳が3体の育成を別々に管理するのは無理なので、パーティごと育つ。')
w()
w('### 追加チケットは上限なし')
w()
w('保護者要望として明確なため、**追加問題によるバトルチケット取得に1日の上限を設けない**。')
w('「進化させたい」「レベルを上げたい」という理由で大量に勉強するなら、それを止めない。')
w()
w('### 難易度は「回数」ではなく「習熟度」で上げる')
w()
w('> **何が問題だったか。** 旧仕様「同じ科目を5回やったら学年+1」は粗く、')
w('> 子どもから見て「頑張った罰として難しくなる」ように感じられかねない。')
w()
w('直近の正答率を見て、**できるようになった分だけ**難しくする。')
w()
w('| パラメータ | 値 |')
w('|---|---|')
w(`| 見る範囲 | 直近${DIFFICULTY.windowSize}問 |`)
w(`| 難しくする条件 | 正答率 ${(DIFFICULTY.upThreshold*100).toFixed(0)}% 以上（最低${DIFFICULTY.minAttemptsBeforeUp}回は試行してから） |`)
w(`| やさしくする条件 | 正答率 ${(DIFFICULTY.downThreshold*100).toFixed(0)}% 以下（罰ではなく調整として） |`)
w()
w('子どもから見た体験は「できるようになったから次へ進んだ」。回数で機械的に上げないので、')
w('得意な子がいつまでも簡単な問題を繰り返す状態にも、苦手な子が置き去りにされる状態にもならない。')
w()
w('### 不正対策（連打・当てずっぽう防止）')
w()
w('| 対策 | 内容 |')
w('|---|---|')
w(`| 複合判定 | 直近${DIFFICULTY.antiSpam.signalWindow}問で複数の不自然シグナルが${DIFFICULTY.antiSpam.minimumSignalsToHoldBonus}個以上重なった場合のみ追加ゲーム報酬を一時保留 |`)
w('| 高速正答/同じ番号 | **単独では罰しない**。学習XPは通常どおり付与 |')
w('| やさしい問題の周回 | 上の習熟度ベースの難易度上昇そのものが歯止めになる（同じ問題を繰り返すほど難しくなるので、周回のうまみが薄れる） |')
w()
w('---')
w()

// ===== 10. 進化日数の検証 =====
w('## 10. 進化までの日数（検証結果・レベル進化＋アイテム進化 全155件）')
w()
w('捕獲レベル（そのエリアの野生 下限）から、標準 ' + DAY.std.xp + ' XP/日 で進化レベルに到達するまでの日数。')
w('**進化レベルは日数目標から逆算して決めた。** 先にレベルを決めてから日数を説明したのではない。')
w()
{
  const BAND = { starter3_first:[2,4], starter3_final:[10,14], common3_first:[3,7], common3_final:[14,28],
    rare3_first:[3,7], rare3_final:[14,28], epic3_first:[7,14], epic3_final:[21,40],
    legend3_first:[7,14], legend3_final:[21,40], two_stage:[8,14] }
  const LABEL = { starter3_first:'スターター 1回目', starter3_final:'スターター 最終', common3_first:'ふつう3段 1回目',
    common3_final:'ふつう3段 最終', rare3_first:'めずらしい3段 1回目', rare3_final:'めずらしい3段 最終',
    epic3_first:'とてもめずらしい 1回目', epic3_final:'とてもめずらしい 最終', two_stage:'2段階（1回だけ）' }
  const bandKey = (f, idx) => f.members.length === 2 ? 'two_stage'
    : `${f.starter ? 'starter' : f.members[0].rank}3_${idx === 0 ? 'first' : 'final'}`
  const rows = [], itemSteps = []
  for (const f of FAMILIES) {
    if (f.members.length < 2) continue
    const start = WILD[f.area][0]
    let prev = start
    f.members.slice(0,-1).forEach((m,i) => {
      const [k,v] = m.evo.split(':')
      if (k !== 'lv') { itemSteps.push({ f, item: v, kind: k, atLv: prev }); return }
      rows.push({ band: bandKey(f,i), days: xpBetween(start, +v)/DAY.std.xp, lv:+v })
      prev = +v
    })
  }
  const grp = {}; rows.forEach(r => (grp[r.band] ||= []).push(r))
  w('### レベル進化 123件')
  w()
  w('| 区分 | 件数 | 実際の日数 | 目安 | 判定 |')
  w('|---|---|---|---|---|')
  for (const k of Object.keys(LABEL)) {
    const list = grp[k]; if (!list) continue
    const d = list.map(r=>r.days), b = BAND[k]
    const ok = d.every(x => x >= b[0]-0.6 && x <= b[1]+0.6)
    w(`| ${LABEL[k]} | ${list.length} | ${Math.min(...d).toFixed(1)}〜${Math.max(...d).toFixed(1)}日 | ${b.join('〜')}日 | ${ok?'**PASS**':'FAIL'} |`)
  }
  w()
  w(`**レベル進化 ${rows.length}件すべてが目安の中に入っている。**`)
  w()
  w(`### アイテム進化 ${itemSteps.length}件`)
  w()
  w('> **何が問題だったか。** 「アイテムはその種族の最初の進化でもらえる」という説明は、')
  w('> アイテムそのものが進化条件になっている系列（例: ロウソッコ→トモシビ は きえないシン が')
  w('> 無いと進化できない）と矛盾していた。また `stone:ancient`（いにしえのいし）は')
  w('> 「エリア4のボス・30日目以降」に紐づいていたが、実際に使うカセキーノはエリア2の')
  w('> ふつうランクで、1か月近く進化できない状態になっていた。')
  w()
  w(`> 修正: アイテムの入手条件を **「対応エリアに入った瞬間から探索ドロップで入手できる」** に統一。`)
  w('> 入手可能エリアは「そのアイテムを使う全系列のうち最小のエリア番号」として`items.mjs`で機械算出するため、')
  w('> 「進化に必要なアイテムが、進化より後にしか手に入らない」矛盾は構造的に起きなくなった。')
  w()
  w(`> ${ACQUIRE_RULE}`)
  w()
  w('| いし・もちもの | 入手エリア | そのエリアに入るまでの目安日数 | 使用例 |')
  w('|---|---|---|---|')
  const allLabels = { ...STONE_LABEL, ...HOLD_LABEL }
  const byItem = {}; itemSteps.forEach((s) => { (byItem[`${s.kind}:${s.item}`] ||= []).push(s) })
  for (const [key, list] of Object.entries(byItem)) {
    const [kind, id] = key.split(':')
    const label = kind === 'stone' ? STONE_LABEL[id] : HOLD_LABEL[id]
    const area = UNLOCK_AREA[key]
    w(`| ${label} | エリア${area} | ${AREA_UNLOCK_DAY[area].toFixed(1)}日目〜 | ${list.map((s) => s.f.members[0].name).slice(0, 3).join('、')} |`)
  }
  w()
  const worstCase = Object.entries(byItem).map(([key]) => AREA_UNLOCK_DAY[UNLOCK_AREA[key]]).reduce((a, b) => Math.max(a, b), 0)
  w(`もっとも遅いアイテム（エリア4限定）でも、最短 ${worstCase.toFixed(0)}日目には入手ルートが開く。`)
  w('カセキーノ（エリア2・ふつう）が必要とする いにしえのいし は、')
  w(`エリア2入場（${AREA_UNLOCK_DAY[2].toFixed(1)}日目）から入手可能——**「1か月近く進化できない」問題は解消した。**`)
  w()
  w('もちものは「持っているのに気づかない」が起きやすい。')
  w('**探索などで入手した瞬間に「○○を みつけた！ このこに もたせられるよ！」**と出す。')
  w('進化後に使用済みアイテムを「てにいれた」とは表示しない。')
}
w()
w('---')
w()

// ===== 11. ギガ・バースト =====
w('## 11. ギガシンカ／キョダイバーストのバトル上の扱い')
w()
w('> **何が問題だったか。** データ上 `GIGA_SHINKA`（12体）と `KYODAI_BURST`（8体）は重複0で、')
w('> 「種族ごとにどちらか一方だけ」がすでに成立していたのに、旧・画面案は')
w('> 「この子をギガにする？キョダイにする？」と**同じ1体について選ばせる**ように描かれていた。')
w('> 正しいUIは「パーティの誰の切り札を使うか」を選ぶもの。加えて、ギガの性能表現が')
w('> 「HP+50/攻撃+50」「全能力×1.35」「ちから+50%」の3通り併存していたのも1つに統一した。')
w()
w('| | ギガシンカ | キョダイバースト |')
w('|---|---|---|')
w(`| 対象 | 最終形 ${GIGA_SHINKA.length}体（12タイプ・1タイプ1体） | 最終形 ${KYODAI_BURST.length}体 |`)
w('| 対象種族の重複 | **0（種族ごとに排他）** | 同左 |')
w('| 必要なもの | ギガキー ＋ その子のギガコア | バーストのしるし |')
w('| アイテムの消費 | **しない**（永久） | **しない**（永久） |')
w('| 回数 | パーティ全体で1バトルに1回 | 同左 |')
w('| 続く長さ | **バトル終了まで** | **3ターン** |')
w(`| ステータス | **全ステータス ×${GIGA_STAT_MULT}**（この1つだけ。他の表現は使わない） | たいりょく ×${BURST_STAT_MULT_HP}／こうげき ×${BURST_STAT_MULT_ATK} |`)
w(`| わざ | そのまま（威力が上がる） | 主力わざが **バーストわざ（いりょく${BURST_MOVE.power}・命中${Math.round(BURST_MOVE.accuracy*100)}%・反動なし）** に置き換わる |`)
w('| 戻ったとき | バトル終了時に通常へ。HP割合を保持 | 3ターン後にHP割合を通常最大HPへ戻す。**0HPは0のまま** |')
w('| 向いている場面 | 長期戦・確実に勝ちたい | ボスの大技前・一気に押し切る |')
w()
w('**種族ごとにギガシンカかキョダイバーストのどちらか一方だけを持つ。**')
w('パーティにギガ対象・バースト対象が両方いることはあるが、')
w('**1バトルで使える特殊変身は合計1回だけ**。プレイヤーが選ぶのは')
w('「この子をギガにするかバーストにするか」ではなく、**「今回の切り札を、どのモンスターに使うか」**。')
w('画面上部に「このバトルで つかえる とくべつなちからは 1つだけ！」と常時出す。')
w()
w('### バーストわざが「使うと損する技」にならないことの検算')
w()
{
  const evBurst = BURST_MOVE.power * BURST_MOVE.accuracy
  const evNormal = NORMAL_FINAL_MOVE.power * NORMAL_FINAL_MOVE.accuracy
  w(`EV(バーストわざ) = ${BURST_MOVE.power} × ${BURST_MOVE.accuracy} = **${evBurst.toFixed(1)}**`)
  w(`EV(通常の主力わざ) = ${NORMAL_FINAL_MOVE.power} × ${NORMAL_FINAL_MOVE.accuracy} = **${evNormal.toFixed(1)}**`)
  w()
  w(`**${evBurst.toFixed(1)} > ${evNormal.toFixed(1)}** なので、反動を無くしただけで通常わざを上回る。`)
  w('「3ターン限定」という制約自体がすでにコストなので、わざ自体に追加の反動は付けない。')
}
w()
w('### キョダイバースト解除時のHP')
w()
w('変身中のHP割合を通常最大HPへ戻す。0HPは0HPのままで、最低1HP保証は入れない。')
w()
w('---')
w()

// ===== 12. 1日の流れ =====
w('## 12. 1日の流れ（rewards.mjs と一致）')
w()
w('| | できごと | もらえるもの |')
w('|---|---|---|')
w('| 1 | コアタスクを1つクリア（×5、22問） | 約170 XP ＋ **ほしのわ +1×5** |')
w('| 2 | その日のコアタスク5つを全部クリア | **バトルチケット +3 ＋ ぎんのわ +1** |')
w('| 3 | バトル（基本3戦。追加学習で得たチケット分は追加可） | 各 40〜80 XP |')
w('| 4 | 勝ったら「わ」を選んで投げる（§8） | 成功したら なかまが増える |')
w('| 5 | 追加問題を1つクリア（上限なし） | **バトルチケット +1** |')
w('| 6 | 追加問題4問セットで3問以上正解（上限なし） | **きんのわ +1** |')
w('| 7 | 7日連続で学習した日 | **つきのいし +1** |')
w('| 8 | 章末テストに合格 | **にじのわ +1** |')
w('| 9 | エリアボスを初めて倒す | **にじのわ +1 ＋ そのエリアのいし1種 +1** |')
w()
w('- **チケットは7日で失効する**（毎日やる動機）')
w('- **「わ」は絶対に失効しない**（ためて使う楽しみ）')
w('- チケットが0でも、**すでに持っている子の育成と図鑑は見られる**')
w('- 追加問題（5・6）は**1日の上限を設けない**。ただし §9 の習熟度ベース難易度上昇が歯止めになる')
w()
w('### 「戦えるのに わ が無い」を起こさない')
w()
w('標準の学習では基本チケット3枚を得る。追加問題で得たチケットも同日に使えるため、3戦はハード上限ではない。')
w('**ふつうランクを捕まえるぶんには常に足りる。**')
w('足りなくなるのは「めずらしい以上にきんのわを連投した」ときだけで、')
w('それは自分で選んだ結果なので納得できる。')
w()
w('---')
w()

// ===== 13. 実装順 =====
w('## 13. 実装順（縦切り検証）')
w()
w('> **何が問題だったか。** 旧・縦切り18体にはギガシンカ対象は含まれていたが、')
w('> キョダイバースト対象が0体だった。239体すべてを作る前に、捕獲・育成・進化・ギガシンカ・')
w('> キョダイバーストまで一通り実機検証する必要があるため、**キョダイバースト対象の系列を1つ追加**する。')
w()
w('最初に作るのは 7系列・21体。')
w()
w('| 系列 | タイプ | 検証したいこと |')
w('|---|---|---|')
w('| モコハ → ワカバネ → ジュランガ | くさ | スターター・レベル進化・**ギガシンカ** |')
w('| ヒノポ → メラガミ → グレンドウ | ほのお | スターター・タイプ一致 |')
w('| シズク → ミナモリ → ワダツラ | みず | スターター・ガードロール |')
w('| コロムシ → マユゴモリ → ニジアゲハ | むし | **早い進化**（1回目 ' + (xpBetween(WILD[1][0], 19)/DAY.std.xp).toFixed(1) + '日相当） |')
w('| ネッコロ → ハナコロ → ハルミドリ | くさ | **いし進化** |')
w('| ネジコロ → ハガネット → テツガイア | はがね | **もちもの進化** |')
w('| モリノコ → ジュモクン → センジュガ | くさ | **キョダイバースト**（旧縦切りに欠けていた） |')
w()
w('この21体で、学習 → チケット → わ → バトル → 相性 → 捕獲 → 捕獲失敗 → 図鑑 → ボックス →')
w('レベルアップ → 進化 → ボス → ギガシンカ → キョダイバースト まで通す。')
w('**ここが面白くなければ、残り218体の絵を描いても意味がない。**')
w()
w('---')
w()

// ===== 14. 長期運用の簡易シミュレーション =====
w('## 14. 長期運用の簡易シミュレーション')
w()
w('手持ち3体だけで、84系列すべてを最終形まで育て切るとどれくらいかかるか。')
w('学習XPは手持ち3体全員に入る前提（現行仕様のまま。「そだてる枠」は追加しない）で、')
w('3体を並行して回し続けるジョブスケジューリングとして簡易シミュレーションした。')
w()
{
  const { completions, totalFamilies, singles } = simulateDexCompletion()
  w(`総系列は ${FAMILIES.length}。このシミュレーション対象は **進化系列 ${totalFamilies}系列**。単体${singles}系列は進化工程がなく最初から完成状態なので除外（${totalFamilies}+${singles}=${FAMILIES.length}）。`)
  w()
  w('| 経過日数 | 完成した系列 | 割合 |')
  w('|---|---|---|')
  for (const days of [30, 90, 180, 365]) {
    const done = completions.filter((c) => c.finish <= days).length
    w(`| ${days}日 | ${done}系列 | ${(done / totalFamilies * 100).toFixed(0)}% |`)
  }
  const last = completions[completions.length - 1]
  w()
  w(`**全${totalFamilies}系列を完成させるまで、目安で約${last.finish.toFixed(0)}日（約${(last.finish / 30).toFixed(1)}か月）。**`)
}
w()
w('1年経っても図鑑の完全制覇までは届かない見込みで、**進化待ちがかなり詰まる**。')
w('ただし、これは「毎日フルで遊び続ける想定」でもこの程度、というだけであり、')
w('v1で緊急に対処すべき問題とまでは考えていない（エリア1〜2だけでも数十種類は現実的な期間で遊べる）。')
w()
w('**v1では対応しない。** 将来、体感的に進化待ちが辛いという声が出た場合の案として、')
w('「そだてる枠」を1〜2体追加し、学習XPの一部（例: 30%）をそちらにも配分する仕組みを検討する。')
w('手持ち3体の戦闘バランスには影響させない（そだてる枠はバトルに出せない、育成専用の枠にする）。')
w()
w('---')
w()
w('## 15. この文書の数値の出どころ')
w()
w('```bash')
w('node scripts/battle.mjs        # XP曲線・ステータス・ダメージ・進化日数を再計算')
w('node scripts/capture.mjs       # 捕獲の1投／3投累積成功率')
w('node scripts/items.mjs         # いし・もちものの入手エリア')
w('node scripts/wildEncounter.mjs # 野生出現プールの分類・rank軸の分離')
w('node scripts/simulate.mjs      # 長期運用の簡易シミュレーション')
w('node scripts/check2.mjs        # 図鑑データの整合性（全項目）')
w('node scripts/gen-battle-md.mjs # この文書を再生成')
w('```')
w()
w('捕獲式 `P = (a/255)^(3/4)` は本作の確率モデルとして固定し、')
w('a = 10 / 45 / 90 / 135 / 180 / 200 / 254 の代表点で計算結果を回帰テストする。')
w('18タイプ相性表は 324通りを機械検算（ばつぐん51／いまひとつ61／こうかなし8／等倍204）。')
w()

fs.writeFileSync(new URL('../06-battle-and-progression-design.md', import.meta.url), L.join('\n'))
console.log('06-battle-and-progression-design.md:', L.join('\n').length, 'bytes /', L.length, 'lines')
