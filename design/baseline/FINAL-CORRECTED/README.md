# FINAL-CORRECTED Baseline

## Purpose

This directory is reserved for the preserved contents of `mana-evo-terra-FINAL-CORRECTED(3).zip`.

**This is a historical, pre-change BASELINE. It is NOT the CURRENT canonical specification.**

The preserved baseline exists so later rebuild work can compare the original design against later approved decisions, current design documents, data masters, and runtime behavior. Nothing in this directory becomes current behavior merely because it existed in the baseline.

## Preservation rules

- Imported source files from the archive must be preserved without editorial modification.
- Do not use this baseline to mechanically roll current behavior back.
- Do not resolve product questions (including 238/239 scope) inside the baseline rescue work item.
- Integrity metadata generated by the rescue process must be kept separately from the preserved source payload.
- Any later canonical decision belongs under `design/rebuild/` or `design/current/`, not by rewriting the baseline.

## Expected integrity records

After the archive is available and fully imported, this directory will include integrity records sufficient to verify completeness, including the original file inventory and cryptographic hashes, plus a comparison listing baseline files that do not exist in current `design/`.

## Current rescue status

**BLOCKED INPUT — archive payload not yet available to this worker.**

The task specifies `mana-evo-terra-FINAL-CORRECTED(3).zip`, but the archive bytes are not currently retrievable from the active chat file workspace and no copy of the archive was found in this repository. Therefore no source file from the original archive has been imported yet, and no source-file manifest has been fabricated.

- Preserved original source files: **0**
- Original-source manifest: **not generated**
- Missing-content assessment: **cannot be completed until the archive is retrievable**

Once the exact archive becomes available, W-001 should resume on the same branch/PR, import the archive without modifying source payloads, generate the manifest, compare against current `design/`, and then replace this status section with the completed integrity result.
