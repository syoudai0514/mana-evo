name: CI

on:
  push:
    branches: [main, 'terra/**', 'rebuild/**']
  pull_request:
    branches: [main, rebuild/canonical-governance]

jobs:
  test-and-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: Canonical design sync gate
        if: github.event_name == 'pull_request'
        env:
          CANONICAL_SYNC_BASE: ${{ github.event.pull_request.base.sha }}
          CANONICAL_SYNC_PR_BODY: ${{ github.event.pull_request.body }}
        run: npm run verify:canonical-sync
      - name: Install dependencies
        run: npm ci
      - name: Audit dependencies
        run: npm audit --audit-level=high
      - name: Unit and integration tests
        run: npm test
      - name: Build
        run: npm run build
      - name: Release readiness artifact
        run: npm run verify:release
      - name: Install WebKit browser
        run: npx playwright install --with-deps webkit
      - name: iPhone WebKit E2E
        run: npm run test:e2e
